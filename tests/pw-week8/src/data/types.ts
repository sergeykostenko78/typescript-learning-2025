export interface UserTestData {
    username: string;
    password: string;
    shouldLogin: boolean;
    description: string;
  }

export interface ProductTestData {
    name: string;
    expectedPrice: string;
  }